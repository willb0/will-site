---
import Layout from "@layouts/Layout.astro";
import ProjectCard from "@components/ProjectCard.astro";
import projects from "@content/projects.json";

const metadata = {
  title: "Projects - Will Bell",
  description: "This is my project section. This is a list of projects I own and contribute to.",
}
---

<Layout {...metadata}>
  <section>
    <h1 class="mb-6 text-3xl md:text-4xl font-semibold tracking-tight">Projects</h1>
    <p class="mb-5 text-zinc-300">
      Here is a list of some projects I've worked on, or I am actively working on.
    </p>
    <section>
      <ul class="mb-5 grid sm:grid-cols-2 gap-5">
        {projects.map((project) => <ProjectCard title={project.title} description={project.description} githubUrl={project.githubUrl} technologies={project.technologies} stargazers={project.stargazers} />)}
      </ul>
    </section>
  </section>
</Layout>
